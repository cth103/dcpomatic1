Thumbnails are poorly named
x-thread signaller
Restartable jobs somehow
use something other than TIFF
More logging

shared_ptr for Parameters, Options
use a FilmState inside Film and use that for parameters

options summary